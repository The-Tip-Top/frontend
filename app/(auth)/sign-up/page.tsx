import AuthForm from '@/components/auth/AuthForm';

const SignIn = () => {
  return (
    <section className="flex-center px-3 sm:px-6">
      <AuthForm type="sign-up" />
    </section>
  );
};

export default SignIn;
