export const publicRoutes = ['/home', '/contact', '/login', '/cadeau'];

export const authRoutes = ['/sign-up', '/error'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_REDIRECT_LOGIN = '/home';
