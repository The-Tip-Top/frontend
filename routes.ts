export const publicRoutes = ['/home', '/new-verification', '/admin'];

export const authRoutes = ['/sign-up', '/sign-in', '/error'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_REDIRECT_LOGIN = '/home';
