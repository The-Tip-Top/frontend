export const publicRoutes = ['/home', '/contact', '/login', '/cadeau', '/new-verification', '/'];

export const authRoutes = ['/sign-up', '/sign-in', '/error'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_REDIRECT_LOGIN = '/settings';
